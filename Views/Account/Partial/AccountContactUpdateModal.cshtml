@inject ResManager resManager
@model AccountViewModel
@using static GSCrm.CommonConsts

<div id="accContactUpdateModal" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">@resManager.GetString("ContactUpdate")</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form method="post" asp-controller="@ACC_CONTACT" asp-action="Update">
                    <input id="accContactId" hidden="hidden" />
                    <div class="mt-3">
                        <input id="updateAccContactFName" class="form-control" type="text" placeholder="@resManager.GetString("FirstNameLabel")" />
                        <ul id="updateAccContactFNameError" class="d-none under-field-error label-sm mt-2"></ul>
                    </div>
                    <div class="mt-3">
                        <input id="updateAccContactLName" class="form-control" type="text" placeholder="@resManager.GetString("LastNameLabel")" />
                        <ul id="updateAccContactLNameError" class="d-none under-field-error label-sm mt-2"></ul>
                    </div>
                    <div class="mt-3">
                        <input id="updateAccContactMName" class="form-control" type="text" placeholder="@resManager.GetString("MidNameLabel")" />
                        <ul id="updateAccContactMNameError" class="d-none under-field-error label-sm mt-2"></ul>
                    </div>
                    <div class="row mt-3">
                        <div class="col">
                            <div class="block-center">
                                <p class="label-md label-non-select">@resManager.GetString("ContactType")</p>
                            </div>
                        </div>
                        <div class="col dropdown-area">
                            <input id="updateAccContactType" class="current-dropdown-value" hidden="hidden" />
                            <span class="dropdown-el">
                                <input type="radio" name="contactType" value="None" checked="checked" id="updateContactTypeNone">
                                <label for="updateContactTypeNone">@resManager.GetString("None")</label>
                                <input type="radio" name="contactType" value="Personal" id="updateContactTypePersonal">
                                <label for="updateContactTypePersonal">@resManager.GetString("Personal")</label>
                                <input type="radio" name="contactType" value="Work" id="updateContactTypeWork">
                                <label for="updateContactTypeWork">@resManager.GetString("Work")</label>
                            </span>
                        </div>
                        <ul id="updateAccContactTypeError" class="under-field-error label-sm mt-3 text-center d-none" style="width: 100%"></ul>
                    </div>
                    <div class="mt-3">
                        <input id="updateAccContactPhone" class="form-control" type="text" placeholder="@resManager.GetString("PhoneNumber")" />
                        <ul id="updateAccContactPhoneError" class="d-none under-field-error label-sm mt-2"></ul>
                    </div>
                    <div class="mt-3">
                        <input id="updateAccContactEmail" class="form-control" type="email" placeholder="@resManager.GetString("Email")" />
                        <ul id="updateAccContactEmailError" class="d-none under-field-error label-sm mt-2"></ul>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <input id="updateAccContactBtn" type="submit" class="btn btn-primary" value="@resManager.GetString("Update")" />
                <input id="cancelUpdateAccContactBtn" type="button" class="btn btn-secondary" data-dismiss="modal" value="@resManager.GetString("CancelLabel")" />
            </div>
        </div>
    </div>
</div>